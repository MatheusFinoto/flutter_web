import 'package:flutter/material.dart';

class CustonDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(width: double.infinity, height: 0.1, color: Colors.grey[200],);
  }
}
